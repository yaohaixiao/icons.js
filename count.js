import SYMBOLS from './symbols'

/**
 * 获取图标集图标数量
 * =============================================================
 * @method count
 * @return {number}
 */
const count = () => {
  return SYMBOLS.length
}

export default count
